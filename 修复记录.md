# 修复记录（2026-09-21）

> 本文记录 DGAP 项目各模块的缺陷与修复，用于答辩时说明"方法实现经过严格校验"。
> 结论：5 个已知 bug 已全部修复，另发现并修复了 9 个更深层问题，全流程已跑通。

## 一、问题和要求.txt 列出的 5 个 bug

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 1 | `dgap/sensitivity.py` `_xywh2xyxy` | cx/cy/w/h 错位：`x1 = cx - cy/2`，把 cy 当 w 用了 | 改为 `x1 = cx - w/2`、`y1 = cy - h/2`（同理 `distill.py` 的同名函数） |
| 2 | `dgap/distill.py` 特征蒸馏 | 剪枝后学生通道数 < 教师，`(t-s)` 直接广播报错 | 加 1×1 卷积 projection 层对齐通道 |
| 3 | `dgap/distill.py` `build_dataloader` | 传 `{"train": path}` 残缺字典 | 改用 `check_det_dataset` + `build_yolo_dataset` + `build_dataloader` 完整构建 |
| 4 | `dgap/distill.py` 缺陷掩码 | bbox 用 640 像素坐标直接画在 80×80 特征图 | 掩码按特征图分辨率 H/W 缩放；且正确识别 bbox 为【归一化 cxcywh】先转 xyxy 再缩放 |
| 5 | `dgap/pruner.py` `example_inputs` | 建在 CPU、模型在 CUDA | 建在与模型同设备的 tensor |

## 二、追加发现并修复的问题

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 6 | `dgap/pruner.py` `ratio_dict` | 键用字符串名，torch_pruning 按 module 对象查表 → 层级自适应比例被静默忽略 | 键改为 `nn.Module` 对象 |
| 7 | `dgap/pruner.py` importance 回调 | `group.dep` / `group.idxs` 不存在（group 是 Group 对象）→ 会 AttributeError | 用 `group[0].dep.target.module`、`group[0].idxs` |
| 8 | **torch_pruning 1.6.1 与 ultralytics 8.4.156 不兼容** | C2f 的 `chunk(2,1)` 隐式 split 使依赖图通道映射出错（越界索引） | 新增 `dgap/c2f_v2.py`：C2f→C2f_v2 重参数化（官方 Issue #147 方案） |
| 9 | 剪枝前模型 | `YOLO().model` 参数 `requires_grad=False`，torch_pruning 依赖 grad_fn 做图 trace → 剪出 0 通道 | 剪枝前统一 `requires_grad=True` |
| 10 | `dgap/sensitivity.py` 图像预处理 | `img[:, :, ::-1]` 产生负 stride 视图，`torch.from_numpy` 报错 | 加 `np.ascontiguousarray` |
| 11 | `dgap/distill.py` 检测损失 | `student.loss(batch)[0]` 是 (box,cls,dfl) 三元张量，非标量 → backward 报错 | 加 `.sum()` |
| 12 | 微调流程 | ultralytics 无法重建"结构已改变"的剪枝模型，`YOLO(out).train()` 会按原 yaml 重建 | 直接把剪枝后的 DetectionModel 注入 `DetectionTrainer` |
| 13 | `dgap/distill.py` 教师/学生层名 | 教师是 C2f、学生是 C2f_v2，蒸馏层名对不上 | 蒸馏前把教师也替换为 C2f_v2 |
| 14 | 重载剪枝 checkpoint | 剪枝模型含 C2f_v2，`YOLO()` 重载需 `dgap.c2f_v2` 可 import | `dgap/__init__.py` 注册 + `02_metrics.py`/`13_quantize_export.py` 引入 |

## 三、验证结果（本次跑通）

- 敏感性分析：72 个卷积层（64 + C2f 拆分多出的 8），`model.2.m.0.cv2.conv` 等缺陷敏感层被正确识别。
- 剪枝（ratio=0.3）：uniform 11.14M→6.05M（−45.7%）；dgap 11.14M→4.87M（−56.2%），前向均正常。
- 微调（1 epoch 验证）：best.pt 正常保存到 `runs/finetune/<tag>_r<ratio>/weights/best.pt`，可被 `YOLO()` 重载。
- 指标：重载后 Params/FLOPs/mAP/FPS 正常计算。
- 蒸馏：教师 11.14M + 学生 6.05M，1 epoch 跑通。

> 注：上述是"跑通验证"，用的是小 ratio / 1 epoch。正式实验请按 进度记录.md 的命令完整跑（80 epoch 微调、32 张图敏感性分析）。

## 四、关键约定（后续改动需注意）

1. **剪枝的 `ratio` 是"每层通道剪枝比例"，不是参数压缩比**。通道剪 30%，参数可压缩约 45%（卷积输入/输出通道同时减少的二次效应）。申报书里写"参数量压缩≥60%"时应以 `02_metrics.py` 实测的 `params_M` 为准。
2. **剪枝/蒸馏后模型含 C2f_v2 模块**，任何 `YOLO(path)` 重载都要求项目根在 `sys.path` 且 `dgap.c2f_v2` 可 import。
3. **微调必须用 trainer 注入**（见修复 #12），不能用 `YOLO(out).train()`。
4. **Windows 上 DataLoader 多进程**（`workers>0`）需脚本有 `if __name__ == "__main__"` 保护；为稳妥默认 `workers=0`。
