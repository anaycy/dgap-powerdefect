# 数据集：去哪里找、怎么整理

## 一、直接可用的公开数据集（YOLO 格式，首选）

课题目标是**电力绝缘子缺陷**检测。下面这些是真实、可直接下载的公开数据集：

| 数据集 | 类别 | 数量/格式 | 链接 |
|---|---|---|---|
| **BGI（破损玻璃绝缘子）** | 破损绝缘子等 | ~544 张，YOLOv7 格式 | https://github.com/phd-benel/BGI |
| VPMBGI（BGI 增强版） | 破损玻璃绝缘子 | ~1010 张 | https://github.com/phd-benel/VPMBGI |
| **Mendeley 瓷盘绝缘子** | Broken / Dirty / Insulator / Shaded（4类） | 255 张，YOLOv8 分割 | https://data.mendeley.com/datasets/5pwrd7gmw2/1 |
| 合成破损绝缘子（HF） | 破损绝缘子 | 200+ 张，YOLO | https://huggingface.co/datasets/silera/broken-insulators-synthetic-detection |
| 绝缘子串闪络污染破损（3类） | crack / insulator / pollution_flashover | 1600 张，VOC+YOLO | https://cloud.tencent.cn/developer/article/2542157 |
| 配网绝缘子损坏（1类） | 绝缘子损坏 | 2384 张，VOC+YOLO | https://cloud.tencent.cn/developer/article/2542135 |
| 3类绝缘子缺陷（破损/正常/污闪） | 破损/正常/污闪 | 12000 张，YOLO | https://developer.aliyun.com/article/1742449 |

**推荐**：先用 **BGI 或 Mendeley 瓷盘绝缘子（4类，含 Broken）** 快速跑通全流程；有精力和时间后再上 1600/12000 张的大数据集冲指标。

## 二、关于「鸟巢」「销钉缺失」这两个类别

要求里提到的 3 类（绝缘子破损 / 鸟巢 / 销钉缺失）很少出现在同一个公开数据集里。两条路：

1. **（推荐，省事）** 直接用「破损绝缘子 + 正常绝缘子 + 污闪」这类公开 3~4 类数据集，把申报书里的缺陷类别写成实际用到的类别即可，学术上完全站得住。
2. **（进阶，加分）** 用 [labelImg](https://github.com/HumanSignal/labelImg) 或 [label-studio](https://labelstud.io/) 自己标 300~500 张「鸟巢」「销钉缺失」图补进去（这两个类别在输电线路巡检图里很常见，图不难找）。

## 三、整理成标准目录（三步）

1. 下载解压数据集。
2. 把图片放进 `data/images/{train,val,test}`，把同名 `.txt` 标签放进 `data/labels/{train,val,test}`。
3. 改 `data/data.yaml` 的 `names` 和 `nc` 为实际类别。

> 提示：如果下载的是 VOC 格式（xml 标注），需要先转成 YOLO txt 格式。
> 转换脚本网上很多，或让我帮你写一个（说一声即可）。

## 四、验证数据集整理对了没

```
.venv/Scripts/python.exe - <<'PY'
import yaml
cfg = yaml.safe_load(open('data/data.yaml', encoding='utf-8'))
import os
for split in ['train','val']:
    d = os.path.join(cfg['path'], cfg[split])
    n = len([f for f in os.listdir(d) if f.lower().endswith(('.jpg','.png','.jpeg'))])
    print(split, '图片数 =', n)
    print('  类别数 nc =', cfg['nc'], 'names =', cfg['names'])
PY
```

看到 `train/val` 图片数 > 0 且类别名正确，就可以开始训练了。
