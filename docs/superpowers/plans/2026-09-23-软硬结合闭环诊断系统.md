# 软硬结合·边端自治·闭环诊断系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有 DGAP 从"轻量化检测 demo"升级为「软硬结合、边端自治、闭环诊断」的电力缺陷边缘智能系统，并重写文档门面供发指导老师。

**Architecture:** 四层对标云协智端——硬件层（自研采集板卡+树莓派网关，软实做/硬设计写指南）、边缘算力层（已有剪枝/蒸馏/量化，保留）、平台层（闭环诊断 + 一键部署平台，本次实做）、跨模态层（无配对决策级融合，本次实做）。纯逻辑模块（diagnosis/fusion）用 TDD，胶水脚本（Gradio/MQTT）用 smoke 验证。

**Tech Stack:** Python 3.13 + ultralytics（检测）+ onnxruntime（边缘推理）+ Gradio（界面/平台）+ paho-mqtt/pyserial（网关，可选）+ pytest（单测）。

**Spec:** `docs/superpowers/specs/2026-09-23-软硬结合闭环诊断系统-design.md`（本计划所有设计依据，执行者需同时读它）

## Global Constraints

- 部署平台/演示界面用 **Gradio**（非 Streamlit），复用现有栈。
- 板卡 MCU 用 **ESP32**（非 STM32）。
- 网关推理用 **onnxruntime**（非 TensorRT，树莓派无 CUDA）。
- 新增依赖 `gradio` / `paho-mqtt` / `pyserial` 在 requirements.txt 中**标注"可选"**，不破坏现有环境。
- 类别固定 `["broken_insulator", "missing_pin"]`（nc=2，与 `data/data.yaml` 一致）。
- 所有命令在项目根目录运行，用 `.venv/Scripts/python.exe`。
- 脚本头写 `# -*- coding: utf-8 -*-` + 中文 docstring；scripts/ 下脚本用 `sys.path.insert(0, ...)` 导入 dgap。
- **纯逻辑模块 `dgap/diagnosis.py`、`dgap/fusion.py` 内部不得 import torch/ultralytics**（树莓派只有 onnxruntime，要能单独拷贝这两个文件跑）。
- 提交信息用中文，`feat:` / `docs:` 前缀；**数据集/权重不入库**（.gitignore 已处理）。

---

## 文件结构总览

```
README.md                                    重写（门面，发指导老师）
docs/分工与计划.md                            精简（移除申报书段）
docs/演示界面与申报书模板.md                   吸收申报书细节 + 界面升级闭环
docs/成员2行动指南.md                          新增（硬件+采集板卡+部署）
docs/成员3行动指南.md                          新增（界面/视频/申报书/PPT）
dgap/diagnosis.py                             新增（闭环诊断，纯逻辑）
dgap/fusion.py                                新增（决策级融合，纯逻辑）
tests/test_diagnosis.py                       新增
tests/test_fusion.py                          新增
scripts/30_demo_ui.py                         新增（闭环诊断 demo）
scripts/40_platform.py                        新增（一键部署平台）
scripts/41_gateway.py                         新增（边缘网关，含 dry-run）
scripts/42_board_simulator.py                 新增（采集板卡模拟器）
requirements.txt                              增补可选依赖
```

---

## Task 1: 重写 README.md（项目门面）

**Files:**
- Modify: `README.md`（整份重写）

**Interfaces:**
- Consumes: spec 第 1/2/3 节的痛点、四层架构、子系统设计（照抄措辞）。
- Produces: 可直接发指导老师的门面文档。

- [ ] **Step 1: 重写 README 为以下结构**

按下面的骨架整份替换 `README.md`（内容从 spec 逐段搬，不改措辞）：

```markdown
# 软硬结合·边端自治·闭环诊断的电力缺陷边缘智能系统

> 挑战杯 / 明理杯 · 科技发明制作 B 类 · 信息技术（计算机）
> 一句话：软硬结合，把电力缺陷检测模型轻量化后下沉到边缘设备，
> 并形成「采集 → 检测 → 分级 → 预警 → 工单」的闭环诊断，而非单个检测器。

## 项目定位（对标全国铜奖"云协智端"）
[四层映射表，照抄 spec 第 2 节表格]

## 要解决的四个痛点（对应四个创新点）
1. 边缘算力受限，高精度模型跑不动
2. 通用剪枝/量化对小目标缺陷"一视同仁"，压缩后精度暴跌
3. 可见光+红外融合需要同一场景配对图，但配对数据集不存在
4. 现有方案多是"检测器"，缺"检测→诊断→决策→执行"闭环

## 创新点
1. 缺陷感知自适应轻量化（分层敏感性 + 混合粒度剪枝量化 + 缺陷区域蒸馏）
2. 闭环诊断引擎（分级/定位/预警/工单）
3. 无配对跨模态决策级融合（可见光 + 红外）
4. 软硬结合：自研多源采集板卡 + 树莓派边缘网关
5. 可视化一键部署平台（训练→压缩→导出→评估）

## 技术路线
电力缺陷数据集 → 高精度模型 → 缺陷感知剪枝 → 缺陷蒸馏 → 混合精度量化
→ ONNX 导出 → 边缘网关(采集+检测+诊断) → 告警/工单

## 关键指标（实测为准，见 results/实验对比表.md）
参数量压缩 ≥60%、推理提速 ≥2.2 倍、mAP@0.5 下降 ≤3%、
边缘端到端时延 / 整机功耗（成员2 实测）

## 目录结构
[反映当前 + 规划中的文件，见"文件结构总览"节]

## 快速开始
[保留原 README 阶段一/二/三的命令，精简为：装环境 → 备数据 → 训 baseline → 跑诊断 demo]

## 硬件清单
- 已购：树莓派5(8GB)、普通摄像头
- 待购：红外测温 MLX90640、温湿度 DHT22、振动 SW-420、ESP32（详见 docs/成员2行动指南.md）

## 团队分工
成员1 算法 / 成员2 部署+硬件 / 成员3 界面+材料（详见 docs/分工与计划.md）
```

- [ ] **Step 2: 通读一遍确认无申报书内部细节**（B3/C/D 表、答辩 PPT、消融实验内部安排都不进 README）

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: 重写 README 为软硬结合闭环诊断项目门面"
```

---

## Task 2: 精简 docs/分工与计划.md

**Files:**
- Modify: `docs/分工与计划.md`

**Interfaces:**
- Produces: 只含分工/时间线/协作规范的内部文档。

- [ ] **Step 1: 删除申报书填写段**

删除当前文件里的「四、申报书填写要点」「五、科学性先进性怎么写」两节（这两节内容移到 Task 3 的演示界面与申报书模板.md）。保留：一句话定位、三人分工、时间线、协作规范、关键提醒。

- [ ] **Step 2: 在"一句话定位"处更新为新定位**

把"一句话"改为：
> 软硬结合，把电力缺陷检测模型轻量化下沉到边缘设备，并形成「采集→检测→分级→预警→工单」闭环诊断。

- [ ] **Step 3: 在成员1任务清单补一行**

在成员1清单末尾加：
```markdown
- [ ] （后续）红外数据集获取 + IR 模型训练（决策级融合用，见 README 展望）
```

- [ ] **Step 4: Commit**

```bash
git add docs/分工与计划.md
git commit -m "docs: 精简分工与计划，申报书填写段移到演示界面与申报书模板"
```

---

## Task 3: 更新 docs/演示界面与申报书模板.md

**Files:**
- Modify: `docs/演示界面与申报书模板.md`

**Interfaces:**
- Consumes: Task 2 移出的申报书填写段。
- Produces: 成员3 的完整材料文档（界面 + 申报书）。

- [ ] **Step 1: 界面代码替换为"闭环诊断 demo"引用**

把 §1 的 Gradio 代码整段替换为一段说明 + 指向真实脚本：

```markdown
## 一、演示界面（闭环诊断 demo）

界面已落地为 `scripts/30_demo_ui.py`：上传巡检图 → 选模型 → 检测 → 分级/定位/预警/工单。
运行：
    .venv/Scripts/pip.exe install gradio
    .venv/Scripts/python.exe scripts/30_demo_ui.py
演示要点：拍"检出缺陷 → 显示[紧急]等级 + 定位 + 运维建议工单"，而非只画框。
```

- [ ] **Step 2: 吸收申报书填写要点**

在 §二（申报书模板）末尾追加两节（内容从 Task 2 移过来的原文）：

```markdown
### B3 表填写要点（逐栏）
[原 分工与计划.md「四、申报书填写要点」表格，照搬]

### 科学性先进性怎么写
[原 分工与计划.md「五、科学性先进性怎么写」表格，照搬]
```

- [ ] **Step 3: 补「作品全称」为新名**

把作品全称更新为：**面向电力边缘设备的软硬结合闭环缺陷诊断系统**（若团队定其它名，以团队为准，此处先占位标注）。

- [ ] **Step 4: Commit**

```bash
git add docs/演示界面与申报书模板.md
git commit -m "docs: 演示界面升级为闭环诊断，吸收申报书填写要点"
```

---

## Task 4: 新建 docs/成员2行动指南.md

**Files:**
- Create: `docs/成员2行动指南.md`

**Interfaces:**
- Consumes: 现有 `docs/树莓派部署步骤.md` 内容；板卡协议（Task 10/11 定义）。
- Produces: 成员2（部署+硬件）完整行动指南。

- [ ] **Step 1: 写指南，结构如下**

```markdown
# 成员2 行动指南（部署 + 硬件）

## 0. 已有硬件
- 树莓派5（8GB）+ 普通 USB 摄像头（已购）
- PC 上训练/导出由成员1 完成

## 1. 待购硬件清单（含链接方向 + 预算）
| 硬件 | 用途 | 参考 |
|---|---|---|
| MLX90640 红外测温模块(32×24) | 采集板卡热成像 | 淘宝/立创 |
| DHT22 温湿度传感器 | 环境监测 | 通用 |
| SW-420 振动传感器 | 杆塔振动监测 | 通用 |
| ESP32-DevKitC | 采集板卡 MCU | 乐鑫官方 |
| 面包板/杜邦线/5V 电源 | 原型搭建 | 通用 |

## 2. 采集板卡设计（自研，对标云协智端硬件层）
### 2.1 架构框图（文字）
[ESP32 ← 各传感器；ESP32 → 树莓派网关（UART/USB 或 WiFi/MQTT）]
### 2.2 器件与接口
- MLX90640 → I2C；DHT22 → GPIO；SW-420 → ADC/GPIO
- 供电：树莓派 5V USB 或独立 5V
### 2.3 通信协议（板卡 → 网关，JSON）
协议名 dgap-board/v1，字段见 scripts/42_board_simulator.py 的 sample()（照抄字段）
### 2.4 若打样 PCB
原理图/PCB 设计要点、打样流程（嘉立创 EDA），打样做不了就用 ESP32+面包板原型
### 2.5 固件
用 Arduino IDE 或 ESP-IDF 写：读传感器 → 组 JSON → UART/MQTT 上报

## 3. 树莓派部署流程
[吸收现有 docs/树莓派部署步骤.md 全部内容，原样并入]

## 4. 网关 app 运行（scripts/41_gateway.py）
- PC 上联调：--dry-run 走通全链路
- 树莓派上：把 dgap/diagnosis.py 单独 scp 过去（纯逻辑，不依赖 torch），网关脚本也拷过去
- 完整采集→检测→诊断→上报的接线与循环见本节 + 树莓派部署步骤

## 5. 性能测试（写进报告的关键数据）
- 帧率/端到端时延/整机功耗（基线 vs 压缩模型对照）
- 记录方法照搬原 树莓派部署步骤 §6
```

- [ ] **Step 2: Commit**

```bash
git add docs/成员2行动指南.md
git commit -m "docs: 新增成员2行动指南（硬件+采集板卡+部署）"
```

---

## Task 5: 新建 docs/成员3行动指南.md

**Files:**
- Create: `docs/成员3行动指南.md`

**Interfaces:**
- Produces: 成员3（材料）checklist 型指南，指向已有文档。

- [ ] **Step 1: 写指南，结构如下**

```markdown
# 成员3 行动指南（界面 + 材料）

> 所有具体代码/模板都在 docs/演示界面与申报书模板.md，本文是 checklist。

## 1. 演示界面
- [ ] 跑通 scripts/30_demo_ui.py（闭环诊断 demo）
- [ ] 录 3~5 分钟视频：检出缺陷 → [紧急]等级 + 定位 + 工单

## 2. 申报书（B3/C/D 表）
- [ ] 按 docs/演示界面与申报书模板.md §二 填写
- [ ] C 表文献综述（模型压缩/电力缺陷/边缘部署三段）

## 3. 研究报告（8000 字）
- [ ] 按模板结构写（引言/相关工作/方法/实验/前景/参考文献）

## 4. 答辩 PPT（5~8 页）+ 答辩稿
- [ ] 突出：真问题 → 方法改进 → 数据证明；软硬结合闭环是新亮点

## 5. 成果佐证
- [ ] 申请软件著作权（受理通知书即可）
- [ ] 联系导师/电力公司争取真实变电站试点数据（写进 README 展望的落地）
```

- [ ] **Step 2: Commit**

```bash
git add docs/成员3行动指南.md
git commit -m "docs: 新增成员3行动指南（界面+材料 checklist）"
```

---

## Task 6: dgap/diagnosis.py 闭环诊断引擎（TDD）

**Files:**
- Create: `dgap/diagnosis.py`
- Test: `tests/test_diagnosis.py`

**Interfaces:**
- Consumes: 无（纯逻辑）。
- Produces（后续任务依赖，签名固定）:
  - `diagnose(detections, meta=None, img_shape=(640, 640), classes=None) -> list[dict]`
    - `detections`: `list[(cls_id, x1, y1, x2, y2, conf)]`（像素坐标）
    - 返回项键：`cls / level / box / conf / area_ratio / location / suggestion`
    - `level` ∈ {紧急, 严重, 一般, 注意}
  - `render_report(img, diagnoses) -> ndarray`（BGR 标注图）
  - `build_alert(diagnosis, meta=None) -> dict`

- [ ] **Step 1: 装 pytest 并写失败测试**

先装 pytest（仅开发依赖，不进 requirements）：
```bash
.venv/Scripts/python.exe -m pip install pytest
```

写 `tests/test_diagnosis.py`：

```python
# -*- coding: utf-8 -*-
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from dgap.diagnosis import diagnose, grade_defect, build_alert, render_report


def test_grade_missing_pin_urgent():
    # 销钉缺失基础分高，正常置信度应至少"严重"
    assert grade_defect("missing_pin", area_ratio=0.001, conf=0.9) in ("紧急", "严重")


def test_grade_insulator_by_area():
    small = grade_defect("broken_insulator", area_ratio=0.001, conf=0.9)
    big = grade_defect("broken_insulator", area_ratio=0.05, conf=0.9)
    # 大面积破损等级应不低于小面积
    assert big in ("紧急", "严重")
    assert small in ("一般", "注意")


def test_low_conf_downgrade():
    assert grade_defect("missing_pin", area_ratio=0.001, conf=0.3) == "严重"


def test_diagnose_returns_fields():
    dets = [(1, 100, 120, 200, 240, 0.91)]  # cls=1 销钉缺失
    out = diagnose(dets, img_shape=(480, 640))
    assert len(out) == 1
    d = out[0]
    for k in ("cls", "level", "box", "conf", "area_ratio", "location", "suggestion"):
        assert k in d
    assert d["cls"] == "missing_pin"
    assert d["level"] in ("紧急", "严重", "一般", "注意")


def test_locate_uses_meta():
    dets = [(0, 10, 10, 50, 50, 0.8)]
    meta = {"line": "10kV 明珠线", "tower": "T102"}
    d = diagnose(dets, meta=meta, img_shape=(480, 640))[0]
    assert "T102" in d["location"]


def test_locate_fallback_relative():
    dets = [(0, 10, 10, 50, 50, 0.8)]
    d = diagnose(dets, meta=None, img_shape=(480, 640))[0]
    assert "相对位置" in d["location"]


def test_build_alert_json():
    d = diagnose([(1, 1, 1, 9, 9, 0.9)], img_shape=(100, 100))[0]
    a = build_alert(d, meta={"source": "camera"})
    assert a["category"] == "missing_pin"
    assert a["level"] == d["level"]
    assert a["source"] == "camera"


def test_render_report():
    import numpy as np
    img = np.zeros((100, 100, 3), dtype=np.uint8)
    d = diagnose([(1, 10, 10, 40, 40, 0.9)], img_shape=(100, 100))
    out = render_report(img, d)
    assert out.shape == img.shape
```

- [ ] **Step 2: 运行确认失败**

```bash
.venv/Scripts/python.exe -m pytest tests/test_diagnosis.py -v
```
Expected: FAIL（`ModuleNotFoundError: dgap.diagnosis`）

- [ ] **Step 3: 写 `dgap/diagnosis.py`（纯逻辑，不 import torch）**

```python
# -*- coding: utf-8 -*-
"""DGAP 闭环诊断引擎：检测结果 -> 分级 / 定位 / 预警 / 工单。

纯逻辑模块：不 import torch/ultralytics，输入输出都是普通数据结构，
可单独拷贝到树莓派（只有 onnxruntime）使用。
"""
from datetime import datetime

DEFAULT_CLASSES = ["broken_insulator", "missing_pin"]

BASE_RISK = {
    "missing_pin": 4,       # 销钉缺失 = 直接危及运行
    "broken_insulator": 3,  # 绝缘子破损 = 按面积分级
}

SUGGESTIONS = {
    "紧急": "立即安排检修，建议 24 小时内处理",
    "严重": "建议 3 日内安排检修",
    "一般": "纳入下轮巡检重点复查",
    "注意": "登记台账，持续观察",
}

LEVEL_COLORS = {  # BGR
    "紧急": (0, 0, 255),
    "严重": (0, 165, 255),
    "一般": (0, 255, 255),
    "注意": (255, 0, 0),
}


def _area_ratio(box, img_shape):
    w = max(box[2] - box[0], 0.0)
    h = max(box[3] - box[1], 0.0)
    return (w * h) / max(img_shape[0] * img_shape[1], 1.0)


def grade_defect(cls_name, area_ratio, conf):
    score = BASE_RISK.get(cls_name, 2)
    if area_ratio > 0.02:
        score += 1
    if conf < 0.5:
        score -= 1
    if score >= 5:
        return "紧急"
    if score >= 4:
        return "严重"
    if score >= 3:
        return "一般"
    return "注意"


def locate_defect(meta, box, img_shape):
    if meta:
        line = str(meta.get("line", ""))
        tower = str(meta.get("tower", ""))
        gps = meta.get("gps", "")
        if line or tower:
            return (line + tower).strip()
        if gps:
            return str(gps)
    cx = (box[0] + box[2]) / 2 / max(img_shape[1], 1)
    cy = (box[1] + box[3]) / 2 / max(img_shape[0], 1)
    return f"图内相对位置({cx:.2f}, {cy:.2f})"


def build_alert(diagnosis, meta=None):
    return {
        "timestamp": datetime.now().isoformat(timespec="seconds"),
        "category": diagnosis["cls"],
        "level": diagnosis["level"],
        "confidence": round(diagnosis["conf"], 4),
        "location": diagnosis["location"],
        "box": [int(v) for v in diagnosis["box"]],
        "suggestion": diagnosis["suggestion"],
        "source": (meta or {}).get("source", "camera"),
    }


def diagnose(detections, meta=None, img_shape=(640, 640), classes=None):
    classes = classes or DEFAULT_CLASSES
    out = []
    for det in detections:
        cls_id, x1, y1, x2, y2, conf = det
        cls_name = classes[int(cls_id)] if int(cls_id) < len(classes) else f"cls{int(cls_id)}"
        box = (float(x1), float(y1), float(x2), float(y2))
        ar = _area_ratio(box, img_shape)
        level = grade_defect(cls_name, ar, float(conf))
        out.append({
            "cls": cls_name,
            "level": level,
            "box": box,
            "conf": float(conf),
            "area_ratio": round(ar, 5),
            "location": locate_defect(meta, box, img_shape),
            "suggestion": SUGGESTIONS[level],
        })
    return out


def render_report(img, diagnoses):
    import cv2
    img = img.copy()
    for d in diagnoses:
        color = LEVEL_COLORS[d["level"]]
        x1, y1, x2, y2 = [int(v) for v in d["box"]]
        cv2.rectangle(img, (x1, y1), (x2, y2), color, 2)
        label = f"{d['cls']} [{d['level']}] {d['conf']:.2f}"
        cv2.putText(img, label, (x1, max(y1 - 5, 0)),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.5, color, 1)
    return img
```

- [ ] **Step 4: 运行确认通过**

```bash
.venv/Scripts/python.exe -m pytest tests/test_diagnosis.py -v
```
Expected: PASS（8 passed）

- [ ] **Step 5: Commit**

```bash
git add dgap/diagnosis.py tests/test_diagnosis.py
git commit -m "feat: 闭环诊断引擎（分级/定位/预警/工单）"
```

---

## Task 7: dgap/fusion.py 决策级融合（TDD）

**Files:**
- Create: `dgap/fusion.py`
- Test: `tests/test_fusion.py`

**Interfaces:**
- Consumes: 无。
- Produces（后续任务依赖）:
  - `iou(box_a, box_b) -> float`
  - `merge_dets(dets_a, dets_b, iou_thresh=0.5) -> list[tuple]`（重叠同类取高置信，互补保留）
  - `modality_weighted_fuse(vis_dets, ir_dets, vis_weight=1.0, ir_weight=1.0, iou_thresh=0.5) -> list[tuple]`
  - 检测格式统一 `(cls_id, x1, y1, x2, y2, conf)`

- [ ] **Step 1: 写失败测试 `tests/test_fusion.py`**

```python
# -*- coding: utf-8 -*-
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from dgap.fusion import iou, merge_dets, modality_weighted_fuse


def test_iou_identical():
    assert abs(iou((0, 0, 10, 10), (0, 0, 10, 10)) - 1.0) < 1e-6


def test_iou_disjoint():
    assert iou((0, 0, 10, 10), (20, 20, 30, 30)) == 0.0


def test_merge_keeps_higher_conf():
    a = [(1, 0, 0, 10, 10, 0.6)]
    b = [(1, 0, 0, 10, 10, 0.9)]
    out = merge_dets(a, b, iou_thresh=0.5)
    assert len(out) == 1 and out[0][5] == 0.9


def test_merge_keeps_complement():
    a = [(1, 0, 0, 10, 10, 0.8)]
    b = [(1, 50, 50, 60, 60, 0.7)]
    out = merge_dets(a, b, iou_thresh=0.5)
    assert len(out) == 2


def test_fuse_scales_confidence():
    vis = [(1, 0, 0, 10, 10, 0.8)]
    ir = [(1, 0, 0, 10, 10, 0.5)]
    out = modality_weighted_fuse(vis, ir, vis_weight=1.0, ir_weight=0.7, iou_thresh=0.5)
    assert len(out) == 1 and abs(out[0][5] - 0.8) < 1e-6
```

- [ ] **Step 2: 运行确认失败**

```bash
.venv/Scripts/python.exe -m pytest tests/test_fusion.py -v
```
Expected: FAIL（`ModuleNotFoundError: dgap.fusion`）

- [ ] **Step 3: 写 `dgap/fusion.py`（纯逻辑，不 import torch）**

```python
# -*- coding: utf-8 -*-
"""DGAP 无配对跨模态决策级融合。

可见光与红外无法获得同一场景的配对图，故不做特征级融合，
改为决策级融合：两模态各自检测，同类且 IoU 重叠的框取高置信度，
互补框都保留，实现漏检互补。
"""


def iou(box_a, box_b):
    x1 = max(box_a[0], box_b[0])
    y1 = max(box_a[1], box_b[1])
    x2 = min(box_a[2], box_b[2])
    y2 = min(box_a[3], box_b[3])
    inter = max(x2 - x1, 0.0) * max(y2 - y1, 0.0)
    area_a = (box_a[2] - box_a[0]) * (box_a[3] - box_a[1])
    area_b = (box_b[2] - box_b[0]) * (box_b[3] - box_b[1])
    union = area_a + area_b - inter
    return inter / union if union > 0 else 0.0


def merge_dets(dets_a, dets_b, iou_thresh=0.5):
    merged = list(dets_a)
    for det in dets_b:
        replaced = False
        for i, m in enumerate(merged):
            if int(m[0]) == int(det[0]) and iou(m[1:5], det[1:5]) >= iou_thresh:
                if det[5] > m[5]:
                    merged[i] = det
                replaced = True
                break
        if not replaced:
            merged.append(det)
    return merged


def modality_weighted_fuse(vis_dets, ir_dets, vis_weight=1.0, ir_weight=1.0,
                           iou_thresh=0.5):
    vis = [(*d[:5], d[5] * vis_weight) for d in vis_dets]
    ir = [(*d[:5], d[5] * ir_weight) for d in ir_dets]
    return merge_dets(vis, ir, iou_thresh)
```

- [ ] **Step 4: 运行确认通过**

```bash
.venv/Scripts/python.exe -m pytest tests/test_fusion.py -v
```
Expected: PASS（5 passed）

- [ ] **Step 5: Commit**

```bash
git add dgap/fusion.py tests/test_fusion.py
git commit -m "feat: 无配对跨模态决策级融合"
```

---

## Task 8: scripts/30_demo_ui.py 闭环诊断 demo

**Files:**
- Create: `scripts/30_demo_ui.py`

**Interfaces:**
- Consumes: `dgap.diagnosis.diagnose / render_report`（Task 6）。
- Produces: 可运行 Gradio demo。

- [ ] **Step 1: 写脚本（完整代码）**

```python
# -*- coding: utf-8 -*-
"""30_demo_ui.py —— 闭环诊断演示：上传图 -> 检测 -> 分级/定位/预警/工单。

运行：.venv/Scripts/python.exe scripts/30_demo_ui.py
依赖：gradio（可选，.venv/Scripts/pip.exe install gradio）
"""
import os, sys, time
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import numpy as np
import gradio as gr
from ultralytics import YOLO

from dgap.diagnosis import diagnose, render_report
import dgap.c2f_v2  # noqa: F401  注册 C2f_v2，供重载剪枝模型

CLASSES = ["broken_insulator", "missing_pin"]

MODELS = {
    "Baseline(YOLOv8s)": "runs/baseline/yolov8s/weights/best.pt",
    "普通剪枝": "runs/finetune/uniform_r0.5/weights/best.pt",
    "DGAP自适应剪枝": "runs/finetune/dgap_r0.5/weights/best.pt",
    "剪枝+蒸馏": "runs/distilled/student_distilled.pt",
}

_cache = {}


def get_model(name):
    if name not in _cache:
        _cache[name] = YOLO(MODELS[name])
    return _cache[name]


def diagnose_image(image, model_name, conf):
    if image is None:
        return None, "请先上传图片"
    model = get_model(model_name)
    t0 = time.time()
    res = model.predict(image, conf=conf, imgsz=640, verbose=False)[0]
    fps = 1 / (time.time() - t0 + 1e-6)

    img = image if isinstance(image, np.ndarray) else np.array(image)
    dets = []
    if res.boxes is not None and len(res.boxes):
        xyxy = res.boxes.xyxy.cpu().numpy()
        cls = res.boxes.cls.cpu().numpy().astype(int)
        cf = res.boxes.conf.cpu().numpy()
        for i in range(len(xyxy)):
            dets.append((int(cls[i]), *xyxy[i].tolist(), float(cf[i])))

    h, w = img.shape[:2]
    diag = diagnose(dets, meta=None, img_shape=(h, w), classes=CLASSES)
    img_bgr = img[:, :, ::-1].copy()   # RGB -> BGR 供 cv2 画框
    plot_bgr = render_report(img_bgr, diag)
    plot = plot_bgr[:, :, ::-1]        # 回 RGB 供 Gradio

    lines = [f"模型: {model_name}", f"推理: {fps:.1f} FPS", f"检出: {len(diag)} 处缺陷", "---"]
    for d in diag:
        lines.append(f"[{d['level']}] {d['cls']} conf={d['conf']:.2f} @ {d['location']}")
        lines.append(f"   建议: {d['suggestion']}")
    return plot, "\n".join(lines)


with gr.Blocks(title="DGAP 闭环诊断") as demo:
    gr.Markdown("# 电力缺陷边缘智能闭环诊断系统")
    with gr.Row():
        inp = gr.Image(type="numpy", label="上传巡检图")
        with gr.Column():
            model_name = gr.Dropdown(list(MODELS), value=list(MODELS)[0], label="模型")
            conf = gr.Slider(0.05, 0.9, 0.25, label="置信度阈值")
            btn = gr.Button("检测并诊断")
    out_img = gr.Image(type="numpy", label="诊断结果")
    out_txt = gr.Textbox(label="诊断报告 / 工单")
    btn.click(diagnose_image, [inp, model_name, conf], [out_img, out_txt])

if __name__ == "__main__":
    demo.launch()
```

- [ ] **Step 2: 装依赖并启动验证**

```bash
.venv/Scripts/pip.exe install gradio
.venv/Scripts/python.exe scripts/30_demo_ui.py
```
Expected: 浏览器打开 http://127.0.0.1:7860，上传一张 `data/images/test/*.jpg` 能看到分级框 + 工单文本。验证后 Ctrl+C 退出。

- [ ] **Step 3: Commit**

```bash
git add scripts/30_demo_ui.py
git commit -m "feat: 闭环诊断演示界面"
```

---

## Task 9: scripts/40_platform.py 一键部署平台

**Files:**
- Create: `scripts/40_platform.py`

**Interfaces:**
- Consumes: 现有 `scripts/01,10,11,12,13,03` 各脚本（subprocess 调用，复用已调通流程）。
- Produces: Gradio 平台，网页点按钮走通 训练→压缩→导出→报告。

- [ ] **Step 1: 写脚本（完整代码）**

```python
# -*- coding: utf-8 -*-
"""40_platform.py —— 边缘AI轻量化部署平台：一键 训练→压缩→导出→报告。

用 subprocess 调用现有脚本，复用已调通流程（在 PC 上运行，需 GPU）。
运行：.venv/Scripts/python.exe scripts/40_platform.py
"""
import os, subprocess
import gradio as gr

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PY = os.path.join(ROOT, ".venv", "Scripts", "python.exe")


def _run(script, *args):
    cmd = [PY, os.path.join(ROOT, "scripts", script), *args]
    p = subprocess.run(cmd, capture_output=True, text=True, cwd=ROOT)
    return p.returncode, (p.stdout or "") + (p.stderr or "")


def _fmt(tag, code, out):
    return f"[{'OK' if code == 0 else 'FAIL'}] {tag}\n\n{out[-3000:]}"


def train(epochs):
    c, o = _run("01_train_baseline.py", "--epochs", str(epochs))
    return _fmt("训练 baseline", c, o)


def sensitivity():
    c, o = _run("10_sensitivity.py", "--weights", "runs/baseline/yolov8s/weights/best.pt",
                "--data", "data/data.yaml")
    return _fmt("敏感性分析", c, o)


def prune(mode):
    c, o = _run("11_prune_dgap.py", "--weights", "runs/baseline/yolov8s/weights/best.pt",
                "--data", "data/data.yaml", "--mode", mode, "--ratio", "0.5", "--fine-tune")
    return _fmt(f"剪枝({mode})", c, o)


def distill():
    c, o = _run("12_distill.py", "--student", "runs/finetune/dgap_r0.5/weights/best.pt",
                "--teacher", "runs/baseline/yolov8s/weights/best.pt", "--data", "data/data.yaml")
    return _fmt("蒸馏", c, o)


def quantize():
    c, o = _run("13_quantize_export.py", "--weights", "runs/finetune/dgap_r0.5/weights/best.pt",
                "--data", "data/data.yaml")
    return _fmt("量化导出", c, o)


def report():
    c, o = _run("03_make_table.py")
    md = ""
    p = os.path.join(ROOT, "results", "实验对比表.md")
    if os.path.exists(p):
        md = open(p, encoding="utf-8").read()
    return _fmt("生成报告", c, o) + "\n\n" + md


with gr.Blocks(title="DGAP 部署平台") as app:
    gr.Markdown("# 边缘AI轻量化部署平台（一键 训练→压缩→导出→报告）")
    with gr.Row():
        epochs = gr.Slider(10, 200, 100, step=10, label="训练轮数")
        b_train = gr.Button("1. 训练 baseline")
    with gr.Row():
        b_sens = gr.Button("2. 缺陷敏感性分析")
        b_prune = gr.Button("3. DGAP 自适应剪枝")
        b_uni = gr.Button("3b. 普通剪枝(对照)")
    with gr.Row():
        b_dist = gr.Button("4. 缺陷区域蒸馏")
        b_quant = gr.Button("5. 量化 + ONNX 导出")
    with gr.Row():
        b_rep = gr.Button("6. 生成评估报告")
    log = gr.Textbox(label="运行日志 / 报告", lines=25)

    b_train.click(train, [epochs], log)
    b_sens.click(sensitivity, [], log)
    b_prune.click(lambda: prune("defect"), [], log)
    b_uni.click(lambda: prune("uniform"), [], log)
    b_dist.click(distill, [], log)
    b_quant.click(quantize, [], log)
    b_rep.click(report, [], log)

if __name__ == "__main__":
    app.launch()
```

- [ ] **Step 2: 启动验证（只点"6. 生成评估报告"这个无需 GPU 的按钮）**

```bash
.venv/Scripts/python.exe scripts/40_platform.py
```
Expected: 浏览器打开，点"6. 生成评估报告"能输出 `results/实验对比表.md` 内容（若表格还没生成则显示脚本报错，属正常，说明按钮链路通）。Ctrl+C 退出。

- [ ] **Step 3: Commit**

```bash
git add scripts/40_platform.py
git commit -m "feat: 一键部署平台（训练→压缩→导出→报告）"
```

---

## Task 10: scripts/42_board_simulator.py 采集板卡模拟器

**Files:**
- Create: `scripts/42_board_simulator.py`

**Interfaces:**
- Consumes: 无。
- Produces: 板卡协议 `dgap-board/v1` 的 JSON 样例（字段被 Task 11 网关 + 成员2指南复用）。

- [ ] **Step 1: 写脚本（完整代码）**

```python
# -*- coding: utf-8 -*-
"""42_board_simulator.py —— 采集板卡模拟器（无真 PCB 也能跑通全流程）。

按板卡协议 dgap-board/v1 周期发送 JSON：温度/湿度/振动/GPS/杆塔号。
支持 --stdout（打印）或 --mqtt（发到 broker）。
运行：.venv/Scripts/python.exe scripts/42_board_simulator.py --stdout
"""
import argparse, json, random, time

PROTO_VERSION = "dgap-board/v1"


def sample(seq, tower="T102", line="10kV 明珠线"):
    return {
        "ver": PROTO_VERSION,
        "ts": time.time(),
        "seq": seq,
        "line": line,
        "tower": tower,
        "gps": {"lat": round(31.23 + random.random() * 0.01, 5),
                "lng": round(121.47 + random.random() * 0.01, 5)},
        "thermal": {"grid_avg": round(random.uniform(25, 45), 1),
                    "max": round(random.uniform(30, 60), 1)},
        "humidity": round(random.uniform(40, 80), 1),
        "vibration": round(random.uniform(0, 0.5), 3),
    }


def main():
    p = argparse.ArgumentParser()
    p.add_argument("--stdout", action="store_true", help="打印 JSON（默认行为）")
    p.add_argument("--mqtt", action="store_true", help="发到 MQTT broker")
    p.add_argument("--broker", default="localhost")
    p.add_argument("--topic", default="dgap/board")
    p.add_argument("--interval", type=float, default=2.0)
    args = p.parse_args()

    seq = 0
    client = None
    if args.mqtt:
        import paho.mqtt.client as mqtt
        client = mqtt.Client()
        client.connect(args.broker)
    while True:
        line = json.dumps(sample(seq), ensure_ascii=False)
        if client:
            client.publish(args.topic, line)
        print(line, flush=True)
        seq += 1
        time.sleep(args.interval)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行验证**

```bash
.venv/Scripts/python.exe scripts/42_board_simulator.py --stdout
```
Expected: 每 2 秒打印一条 JSON，字段含 `ver/ts/line/tower/gps/thermal/humidity/vibration`。看 3 条后 Ctrl+C 退出。

- [ ] **Step 3: Commit**

```bash
git add scripts/42_board_simulator.py
git commit -m "feat: 采集板卡模拟器（dgap-board/v1 协议）"
```

---

## Task 11: scripts/41_gateway.py 边缘网关（含 dry-run）

**Files:**
- Create: `scripts/41_gateway.py`

**Interfaces:**
- Consumes: `dgap.diagnosis.diagnose / build_alert`（Task 6）；板卡协议字段（Task 10）。
- Produces: 网关 app；`--dry-run` 模式在无硬件下走通 检测→诊断→告警。

- [ ] **Step 1: 写脚本（完整代码）**

```python
# -*- coding: utf-8 -*-
"""41_gateway.py —— 树莓派边缘网关：采集(摄像头+板卡) -> 检测 -> 闭环诊断 -> 上报。

--dry-run：不连摄像头/MQTT，伪造一条检测 + 一条板卡数据走通全链路（PC 可跑）。
完整采集→检测→诊断→上报的接线与循环见 docs/成员2行动指南.md。
运行：.venv/Scripts/python.exe scripts/41_gateway.py --dry-run
"""
import argparse, json, os, sys
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

import numpy as np

from dgap.diagnosis import diagnose, build_alert

CLASSES = ["broken_insulator", "missing_pin"]


def infer(session, img, imgsz=640):
    """letterbox -> ONNX -> 返回 dets [(cls_id,x1,y1,x2,y2,conf)]。"""
    import cv2
    h, w = img.shape[:2]
    r = min(imgsz / h, imgsz / w)
    nw, nh = int(round(w * r)), int(round(h * r))
    resized = cv2.resize(img, (nw, nh), interpolation=cv2.INTER_LINEAR)
    canvas = np.full((imgsz, imgsz, 3), 114, dtype=np.uint8)
    dw, dh = (imgsz - nw) // 2, (imgsz - nh) // 2
    canvas[dh:dh + nh, dw:dw + nw] = resized
    blob = canvas[:, :, ::-1].transpose(2, 0, 1).astype(np.float32) / 255.0
    blob = blob[None]
    out = session.run(None, {session.get_inputs()[0].name: blob})[0]
    pred = out[0].transpose()          # (8400, 4+nc)
    boxes, scores = pred[:, :4], pred[:, 4:]
    dets = []
    for c in range(len(CLASSES)):
        for i in np.where(scores[:, c] > 0.25)[0]:
            x1, y1, x2, y2 = boxes[i]
            x1 = (x1 - dw) / r; y1 = (y1 - dh) / r
            x2 = (x2 - dw) / r; y2 = (y2 - dh) / r
            dets.append((c, float(x1), float(y1), float(x2), float(y2), float(scores[i, c])))
    return dets


def main():
    p = argparse.ArgumentParser()
    p.add_argument("--model", default="runs/export/best_fp32.onnx")
    p.add_argument("--dry-run", action="store_true", help="不连硬件，跑一条样例")
    args = p.parse_args()

    if args.dry_run:
        dets = [(1, 100, 120, 200, 240, 0.91)]  # cls=1 销钉缺失
        meta = {"line": "10kV 明珠线", "tower": "T102", "source": "camera"}
        diag = diagnose(dets, meta=meta, img_shape=(480, 640), classes=CLASSES)
        for d in diag:
            print(json.dumps(build_alert(d, meta), ensure_ascii=False))
        print("[网关] dry-run 走通：检测 -> 诊断 -> 告警")
        return

    print("[网关] 完整采集->检测->诊断->上报 流程见 docs/成员2行动指南.md（含摄像头/板卡接线）")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行验证**

```bash
.venv/Scripts/python.exe scripts/41_gateway.py --dry-run
```
Expected: 打印一条含 `level/建议/杆塔号` 的告警 JSON + 末尾 `[网关] dry-run 走通`。

- [ ] **Step 3: Commit**

```bash
git add scripts/41_gateway.py
git commit -m "feat: 边缘网关（采集+检测+闭环诊断+上报）"
```

---

## Task 12: requirements.txt 增补可选依赖 + 收尾

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: 追加可选依赖（带注释，不破坏现有）**

在文件末尾追加：

```
# ---- 以下为 DGAP 软硬结合/平台/网关的可选依赖，按需装 ----
gradio                      # 演示界面 + 部署平台（30_demo_ui / 40_platform）
paho-mqtt                   # 网关/板卡模拟器 MQTT 上报
pyserial                    # 板卡 UART 串口通信
```

- [ ] **Step 2: 通读 README 目录结构，确认与实际文件一致**（Task 1 写的目录结构若与新增文件不符，就地修正）

- [ ] **Step 3: 全量跑一遍测试**

```bash
.venv/Scripts/python.exe -m pytest tests/ -v
```
Expected: 13 passed（diagnosis 8 + fusion 5）

- [ ] **Step 4: Commit**

```bash
git add requirements.txt README.md
git commit -m "chore: 增补可选依赖，同步 README 目录结构"
```

---

## 自检记录

- **Spec 覆盖**：spec 第 3 节四子系统 → Task 6/8（闭环诊断）、9（平台）、10/11（网关+板卡）、7（融合）；第 4 节文档重组 → Task 1–5；第 5 节交付边界（硬件/红外写文档）→ Task 4/5；第 7 节技术选型 → Global Constraints。
- **占位扫描**：无 TBD/TODO；所有代码任务含完整代码；文档任务含章节骨架 + 关键文案。
- **类型一致性**：检测格式 `(cls_id,x1,y1,x2,y2,conf)` 在 Task 6/7/8/10/11 全一致；`diagnose()` 返回键在 Task 6 定义、Task 8/11 消费，字段名一致。
