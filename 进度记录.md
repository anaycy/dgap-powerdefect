# 项目进度记录（重启后从这里继续）

> 上次更新：2026-09-20。项目：面向电力边缘设备的缺陷感知自适应轻量化检测系统（明理杯/挑战杯 B类）

## 已完成 ✅

| 阶段 | 结果 |
|---|---|
| 环境安装 | `.venv`，torch 2.11.0+cu128，CUDA 可用（RTX 4060） |
| 数据集 | iraset 破损玻璃绝缘子（1类，909训练/101验证）已整理进 `data/` |
| Baseline 训练 | YOLOv8s 100轮，`runs/baseline/yolov8s/weights/best.pt` |
| 指标计算 | Params 11.14M / FLOPs 28.65G / mAP@50 93.66% / FPS 123.7 |
| 第一张实验表 | `results/实验对比表.md` 和 `.csv` |

## 下一步（DGAP 阶段二）

按顺序跑（都在项目根目录 `E:\大学\111明理杯\dgap-powerdefect` 下）：

```bash
# ① 缺陷敏感性分析（模块A 创新点，最稳）
.venv/Scripts/python.exe scripts/10_sensitivity.py --weights runs/baseline/yolov8s/weights/best.pt --data data/data.yaml

# ② 自适应剪枝（defect）+ 普通剪枝（uniform 对照），加 --fine-tune
.venv/Scripts/python.exe scripts/11_prune_dgap.py --weights runs/baseline/yolov8s/weights/best.pt --data data/data.yaml --mode defect --ratio 0.5 --fine-tune
.venv/Scripts/python.exe scripts/11_prune_dgap.py --weights runs/baseline/yolov8s/weights/best.pt --data data/data.yaml --mode uniform --ratio 0.5 --fine-tune

# ③ 缺陷区域知识蒸馏
.venv/Scripts/python.exe scripts/12_distill.py --student runs/finetune/dgap_r0.5/weights/best.pt --teacher runs/baseline/yolov8s/weights/best.pt --data data/data.yaml

# ④ 量化 + ONNX 导出
.venv/Scripts/python.exe scripts/13_quantize_export.py --weights runs/finetune/dgap_r0.5/weights/best.pt --data data/data.yaml
```

每做完一个方法，跑一次：
```bash
.venv/Scripts/python.exe scripts/02_metrics.py --weights <该方法的best.pt> --data data/data.yaml --method "<方法名>"
.venv/Scripts/python.exe scripts/03_make_table.py
```
表格会自动多一行，最终 `results/实验对比表.md` 就是申报书 B3 的核心证据。

## 注意事项
- ②③ 依赖 torch_pruning / ultralytics 内部 API，首次跑可能有小报错，属正常，报错贴给 Claude 一起修。
- 所有命令都在项目根目录下运行，用 `.venv/Scripts/python.exe`。
